import { usePortfolio } from '../../lib/stores/usePortfolio';

// Component has been simplified to not show descriptions as requested
export default function ExhibitDescription() {
  // Return null to render nothing - descriptions have been removed
  return null;
}